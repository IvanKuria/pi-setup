---
description: Plan inline in terminal, do not create markdown plan files
argument-hint: "<task>"
---
For this task: $ARGUMENTS

Operate terminal-first:
- Do not create markdown plan/spec/implementation files unless I explicitly ask.
- First inspect only what is needed.
- Output the plan inline in the terminal/chat as concise numbered steps.
- Ask for approval before editing files if the work affects behavior, data, configs, schemas, archiving, deletion, or production paths.
- If a saved plan artifact would help, ask: "Do you want this saved as a markdown file?"
- Prefer small, reviewable diffs.
